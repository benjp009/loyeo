{
  "name": "@loyeo/api",
  "version": "0.0.1",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "dev": "firebase emulators:start --only functions",
    "build": "tsc",
    "deploy": "firebase deploy --only functions",
    "lint": "eslint src/",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf lib"
  },
  "dependencies": {
    "@loyeo/types": "workspace:*",
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.0.0",
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@loyeo/config": "workspace:*",
    "@types/express": "^4.17.0",
    "@types/node": "^22.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": "22"
  }
}
